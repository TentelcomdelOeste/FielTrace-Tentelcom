name: Restore integrated camera

# DISABLED: This workflow looked for an old "native capture" code path that no longer exists.
# The app now uses integrated react-webcam + Media.savePhoto on main.
# Kept only as manual/workflow_dispatch in case historical recovery is needed.

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  restore-camera:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled notice
        run: |
          echo "This workflow is disabled on push. The integrated webcam + gallery flow is already on main."
          echo "If you need to recover an old code path, update the search/replace blocks in this file first."
          exit 0
