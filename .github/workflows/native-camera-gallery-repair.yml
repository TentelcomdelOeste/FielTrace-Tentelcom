name: One-time native camera and gallery repair (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled; native-camera-gallery-repair-v2 performs the repair."
